sonar.projectKey=VidiomTM_subtitle-correction
sonar.organization=vidiomtm
sonar.qualitygate.wait=true

# Source paths
sonar.sources=subtitle_correction
sonar.tests=tests

# Coverage: the report path is injected per event by the SonarQube workflow;
# every scan (PR and main) downloads the CI artifact before scanning.
